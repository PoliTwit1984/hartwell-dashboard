import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Hartwell Operations Dashboard",
  description:
    "Sample build for Sprint 01 / Code / Advanced — AI Builders Club. Interactive ops dashboard for fictional regional 3PL Hartwell Logistics. Built with Claude Code; click any customer to scope the dashboard, click 'See where the data lives' for the system→widget map, click 'Generate weekly briefing' to watch Claude compose a forwardable HTML memo from current state.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
